<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/activity_welcome_screen"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="a3t.groupartapp.comp3717.artapp.WelcomeScreenActivity">

    <android.support.v7.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        app:popupTheme="@style/ThemeOverlay.AppCompat.Light"
        app:layout_collapseMode="pin" />

    <Button
        android:text="NEAREST ART"
        android:layout_width="150sp"
        android:layout_height="60sp"
        android:layout_marginTop="123dp"
        android:id="@+id/nearestArtButton"
        android:elevation="23dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:onClick="goToNearestArt"/>

    <Button
        android:text="MY COLLECTION"
        android:layout_width="150sp"
        android:layout_height="60sp"
        android:id="@+id/myCollectionButton"
        android:layout_marginTop="117dp"
        android:layout_below="@+id/nearestArtButton"
        android:layout_centerHorizontal="true"
        android:onClick="goToMyCollection"/>

</RelativeLayout>
